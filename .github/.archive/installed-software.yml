name: Installed Software
on: push

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: node --version
      - run: npm --version
      - run: php --version
      - run: composer --version
      - run: python --version
      - run: pip --version
      - run: mysql --version
